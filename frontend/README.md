# Frontend

La UI esta en `src/frontend`, pero el proyecto Next.js se despliega desde la raiz del repositorio.

Este directorio existe solo para dejar una plantilla separada de variables publicas cuando se quiera documentar la capa frontend.

Para desarrollo local, copiar estas variables al `.env.local` de la raiz.
